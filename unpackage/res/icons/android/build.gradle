android {
  buildTypes {
    release {
      minifyEnabled true // 开启原生代码混淆
      shrinkResources true // 移除无用原生资源
      proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
    }
  }
  // 仅保留必要的ABI
  ndk {
    abiFilters 'armeabi-v7a', 'arm64-v8a'
  }
}