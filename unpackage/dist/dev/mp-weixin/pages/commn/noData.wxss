@charset "UTF-8";
/**
 * 这里是uni-app内置的常用样式变量
 *
 * uni-app 官方扩展插件及插件市场（https://ext.dcloud.net.cn）上很多三方插件均使用了这些样式变量
 * 如果你是插件开发者，建议你使用scss预处理，并在插件代码中直接使用这些变量（无需 import 这个文件），方便用户通过搭积木的方式开发整体风格一致的App
 *
 */
/**
 * 如果你是App开发者（插件使用者），你可以通过修改这些变量来定制自己的插件主题，实现自定义主题功能
 *
 * 如果你的项目同样使用了scss预处理，你也可以直接在你的 scss 代码中使用如下变量，同时无需 import 这个文件
 */
/* 颜色变量 */
/* 行为相关颜色 */
/* 文字基本颜色 */
/* 背景颜色 */
/* 边框颜色 */
/* 尺寸变量 */
/* 文字尺寸 */
/* 图片尺寸 */
/* Border Radius */
/* 水平间距 */
/* 垂直间距 */
/* 透明度 */
/* 文章场景相关 */
.empty-container {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 60vh;
  padding: 20rpx;
}
.empty-container .empty-card {
  width: 100%;
  max-width: 500rpx;
  background: #fff;
  border-radius: 24rpx;
  padding: 60rpx 40rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  position: relative;
  overflow: hidden;
}
.empty-container .empty-icon {
  width: 180rpx;
  height: 180rpx;
  margin-bottom: 40rpx;
  opacity: 0.9;
}
.empty-container .empty-wave {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 10rpx;
  overflow: hidden;
}
.empty-container .empty-wave .wave {
  width: 200%;
  height: 100%;
  background: linear-gradient(90deg, rgba(100, 175, 255, 0.5) 0%, rgba(65, 145, 255, 0.8) 50%, rgba(100, 175, 255, 0.5) 100%);
  -webkit-animation: wave 2s linear infinite;
          animation: wave 2s linear infinite;
}
.empty-container .empty-title {
  font-size: 36rpx;
  font-weight: 500;
  color: #333;
  margin-bottom: 16rpx;
}
.empty-container .empty-desc {
  font-size: 28rpx;
  color: #999;
  text-align: center;
  margin-bottom: 40rpx;
}
.empty-container .empty-button {
  background: linear-gradient(135deg, #5b8cff, #3a6eff);
  color: white;
  border: none;
  border-radius: 50rpx;
  padding: 0 60rpx;
  height: 80rpx;
  line-height: 80rpx;
  font-size: 30rpx;
  box-shadow: 0 6rpx 12rpx rgba(58, 110, 255, 0.2);
  transition: all 0.3s;
}
.empty-container .empty-button:active {
  -webkit-transform: scale(0.95);
          transform: scale(0.95);
  opacity: 0.9;
}
@-webkit-keyframes wave {
0% {
    -webkit-transform: translateX(-50%);
            transform: translateX(-50%);
}
100% {
    -webkit-transform: translateX(0%);
            transform: translateX(0%);
}
}
@keyframes wave {
0% {
    -webkit-transform: translateX(-50%);
            transform: translateX(-50%);
}
100% {
    -webkit-transform: translateX(0%);
            transform: translateX(0%);
}
}

